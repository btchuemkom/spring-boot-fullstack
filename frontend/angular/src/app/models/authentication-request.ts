export interface AuthenticationRequest {
  username?: string; //? --> to make username optional. That mean it can be empty
  password?: string; //? --> to make password optional. That mean it can be empty
}
